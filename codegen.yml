overwrite: true
schema: schema.graphql
generates:
  typings/emere-graphql/index.d.ts:
    plugins:
      - 'typescript-common'
      - 'typescript-server'
      - 'typescript-resolvers'
  schema.graphql.json:
    plugins:
      - 'introspection'
